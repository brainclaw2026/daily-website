## 2026-03-23 Durable Notes

- 用户正在维护网站 `https://embodied-ai-daily.vercel.app`，偏好冷静直接的排障与执行风格。
- 今日已修复该站点的自动更新链路：确认本地数据更新后成功 push 到 GitHub，发现 Vercel 项目最初未连接 Git，后续已连接仓库 `brainclaw2026/daily-website`，并在当时将项目 Root Directory 指向旧工作区 `workspaces/fullstack-dev-assistant`（现 agent 工作区已迁移到 `/Users/brain/.openclaw/workspace-fullstack`）。
- 今日已修复 Vercel 部署被拦截问题：将 Next.js / eslint-config-next 从 15.3.0 升级到 16.2.1，并同步修正 `next.config.ts` 与 `next-env.d.ts`，本地 build 通过后已推送。
- 当前固定线上流程：GitHub Actions 每天北京时间 08:00 自动执行采集；若 `data/content-items.json` 有变化则自动 commit + push 到 `main`；Vercel 自动部署；网站自动更新。
- 今日已将 GitHub Actions 定时从北京时间 09:00 调整为 08:00，并补充 workflow 文档与环境变量传递（含 `INGEST_LOOKBACK_DAYS`、`INGEST_MAX_ITEMS_PER_SOURCE`、`OPENROUTER_API_KEY`、`OPENROUTER_MODEL`）。
- 用户确认需要正式补齐 `dataset` / `conference` / `lab` 三类内容采集。当时旧工作区内已开始实现：新增 `src/lib/ingest/sources/datasets.ts`、`conference.ts`、`labs.ts`，并更新分类与 ingest pipeline；本地 `npm run ingest` 后已出现这三类条目。
- 用户明确希望“增加 agents.defaults.timeoutSeconds in your config”，当前尚未执行；当前读取到的本机 OpenClaw 配置中该值为 120 秒，后续应等待用户给出目标值（建议项曾提供 300 / 600 / 1800）。
