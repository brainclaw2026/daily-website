# 2026-03-20 工作记录

## skill-vetter 安装与规则确认
- 已安装技能：`openclaw-skill-vetter`
- 安装位置：`/Users/brain/.openclaw/workspace/skills/openclaw-skill-vetter`
- 用户要求：以后每次安装新 skill 前，都先用 skill-vetter 做检查，再执行安装
- 已将该规则写入 `AGENTS.md`

## skill 去重偏好
- 用户确认：对于重复/可合并的 skills，优先保留 ClawHub 星数（搜索分数）更高的版本
- 当前核对结果：
  - `self-improving` → 保留 `self-improving`（3.854），高于 `self-improving-agent` 系列候选
  - `gog` → 保留 `gog`（4.036）
  - `summarize` → 保留 `summarize`（4.025）

## 已执行清理
- 已按用户确认清理重复 skill：将 `~/.openclaw/skills/self-improving-agent` 移到 `~/.openclaw/skills/.trash-2026-03-20/`
- 保留 `~/.openclaw/skills/self-improving`

## 网站开发 skill 安装
- 用户要求：用 `find-skills` 找网站开发相关 skill，先用 `skill-vetter` 审查，通过后再安装；不用装很多，够用即可
- 检索候选：`web`、`frontend`
- vetting 结果：
  - `web`：低风险，纯文档型指导 skill，无可疑网络/执行/凭据访问模式，适合通用网站开发
  - `frontend`：低风险，偏前端设计/UI 指导，也无明显危险模式，但当前不必一起安装
- 已安装：`web` → `/Users/brain/.openclaw/workspace/skills/web`
- 安装策略：先装 1 个通用网站开发 skill，够用即可，避免一次装太多
